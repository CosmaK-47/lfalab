package org.example;

enum TokenType {
    NUMBER,      // Represents numeric values (integers, floats)
    OPERATOR,    // Represents +, -, *, /
    FUNCTION,    // Represents functions like sin, cos
    PARENTHESIS, // Represents '(' and ')'
    EOF          // Represents the end of input
}
